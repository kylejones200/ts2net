name: Tests

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  test:
    name: Test on Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.12', '3.13']
    
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y libblas-dev liblapack-dev libbz2-dev liblzma-dev
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install maturin pytest pytest-cov PyYAML
          
      - name: Build and install package
        run: |
          # Install the package in editable mode for testing
          # This ensures Python finds the code and the Rust extension
          pip install -e .
          # Install optional dependencies for testing
          pip install numba tslearn pynndescent pyreadr || echo "Some optional deps failed, continuing..."
          
      - name: Install optional dependencies for testing
        run: |
          # Install BSTS dependencies for BSTS tests (required for CI)
          pip install statsmodels
          # Install Polars and pyarrow for Parquet tests (required for CI)
          pip install polars pyarrow
          # Verify installations
          python -c "import statsmodels; print(f'statsmodels {statsmodels.__version__}')"
          python -c "import polars; import pyarrow; print(f'polars {polars.__version__}, pyarrow {pyarrow.__version__}')"
        continue-on-error: false

      - name: Run tests
        run: |
          pytest tests/ --cov=ts2net --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

