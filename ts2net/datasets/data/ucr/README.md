Bundled UCR univariate classification datasets (train/test ``.npz``).

Regenerate with::

    uv run python tools/bundle_ucr_datasets.py

Sources: UCR Time Series Classification Archive via ``aeon`` (Zenodo TSML).
